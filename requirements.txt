# --- N4 Benchmark Toolkit Requirements ---
# Supports both Slurm Cluster and OpenStack VM environments.
# Usage: 
#   pip install -r requirements.txt

# Core Deep Learning & Math
torch
numpy

# Monitoring & Logging
wandb
nvidia-ml-py  # Provides 'pynvml' module for GPU monitoring
psutil        # System monitoring (used in OpenStack benchmarks)
